{
  "name": "website",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "docusaurus": "docusaurus",
    "start": "npm run generate-builtin-functions && docusaurus start",
    "build": "npm run generate-builtin-functions && docusaurus build",
    "swizzle": "docusaurus swizzle",
    "deploy": "docusaurus deploy",
    "clear": "docusaurus clear",
    "serve": "docusaurus serve",
    "write-translations": "docusaurus write-translations",
    "write-heading-ids": "docusaurus write-heading-ids",
    "format": "prettier --write \"**/*.{js,jsx,ts,tsx,json,md}\"",
    "generate-builtin-functions": "node scripts/generate-builtin-functions.js"
  },
  "dependencies": {
    "@docusaurus/core": "2.1.0",
    "@docusaurus/plugin-client-redirects": "2.1.0",
    "@docusaurus/preset-classic": "2.1.0",
    "docusaurus-plugin-image-zoom": "^0.1.1",
    "prism-react-renderer": "1.2.1",
    "react": "^17.0.1",
    "react-dom": "^17.0.1",
    "clsx": "^1.2.1",
    "@fluentui/react-icons": "^2.0.204"
  },
  "browserslist": {
    "production": [
      ">0.5%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "node-fetch": "^2.6.7",
    "prettier": "2.6.0"
  }
}
